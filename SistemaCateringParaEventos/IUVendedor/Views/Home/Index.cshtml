@using IUVendedor.Models
@{
    ViewBag.Title = "Home Page";
    var usuario = Session["usuario"] as Usuario;
}

<main>
    @if (usuario != null)
    {
        <section class="row" aria-labelledby="bienvenida">
            <h2>Bienvenido, @usuario.Nombre!</h2>
            <p class="lead">Has iniciado sesión como <strong>@usuario.Email</strong>.</p>
            <p>Tu rol es: <strong>@usuario.Rol_id</strong></p>
        </section>
    }
    else
    {
        <section class="row" aria-labelledby="noSesion">
            <h2>No has iniciado sesión</h2>
            <p class="lead">Por favor, <a href="@Url.Action("Index", "Acceso")">inicia sesión</a> para continuar.</p>
        </section>
    }

    <hr />

    <section class="row" aria-labelledby="aspnetTitle">
        <h1 id="title">ASP.NET</h1>
        <p class="lead">ASP.NET es un framework gratuito para crear excelentes sitios web y aplicaciones web usando HTML, CSS y JavaScript.</p>
    </section>

    <div class="">
        @if (usuario.Rol_id == Rol.Chef)
        {
            <div class="card">
                <div class="card-header">
                    <i class="fa fa-box"></i> BUZÓN DE SUGERENCIAS
                </div>
                <div class="card-body">
                    <div class="row">
                    </div>

                    <hr />

                    <table id="tablaSugerencias" class="display cell-border" style="width:100%">
                        <thead>
                            <tr>
                                <th>HORA</th>
                                <th>NOMBRE DEL LABURANTE</th>
                                <th>SUGERENCIA</th>
                            </tr>
                        </thead>
                        <tbody>
                            @* AJAX *@
                        </tbody>
                    </table>


                </div>
            </div>
        }
    </div>
</main>
